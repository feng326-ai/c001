# 采集 v2 单机灰度验收记录（2026-08-23）

## 一、结论

`win10-01` 的采集 v2 单机灰度通过，可以继续在当前门禁下生产观察；本次没有授权或执行其余三台 VM 扩量。

建议保持单机灰度 12～24 小时。观察期内若无本机 fence、heartbeat、result 异常，再按同样的“排空 → 禁领 → 切换 → 单机放行 → 长租约观察 → 自然结果回传”流程逐台升级 `win10-02/03/04`，禁止四台同时切换。

## 二、变更范围

- Ubuntu 采集服务端：仅重建 `wxsearch_worker`，未重建 PostgreSQL、Redis、beat、backend 或其他容器。
- Worker 只读源码挂载：`/home/m/releases/75c451ff4e90a7eda849b80c043c7378e2f4c106/wxsearch`。
- Worker 协议门禁：`CLAIM_PROTOCOL_FLOORS=win10-01:2`。
- 灰度 VM：`win10-01`；验收时域 MAC 为 `52:54:00:fe:94:39`，DHCP IP 为 `192.168.122.156`。
- 未改动 Ubuntu 脏工作目录 `/home/m/xiansuo`，未改动 PostgreSQL/Redis 数据卷。

## 三、自动化门禁

提交 `75c451ff4e90a7eda849b80c043c7378e2f4c106` 的 48 项测试全部通过，其中 12 项在临时隔离的 PostgreSQL 15 + Redis 7 上执行真实集成测试。测试环境及临时网络在结束后已清理。

## 四、灰度证据

### Phase A：排空与禁领

- 先把 `win10-01` 门禁提升到协议 floor 3，旧进程已有租约自然完成，没有强杀进行中的关键词。
- 排空确认同时满足：Redis 无该节点 owner、PostgreSQL `devices.current_keyword` 为空、注册渠道匹配、服务端 floor 3 生效。
- 候选进程在 floor 3 下成功产生两个相互独立的 heartbeat 时间戳，但没有领取新任务。
- 切换后只保留一个采集进程；旧 PID 为 1000，新 PID 为 1596。

### Phase B：v2 单机放行

- 服务端门禁降为 `win10-01:2` 后，首批领取 3 个关键词，均带非空 `lease_id`，队列成员均使用 `v2|souyisou|...` 格式。
- 同一租约连续观察 660 秒，记录到 12 个不同的续租时间戳；超过 600 秒边界后 owner 与关键词渠道状态仍保持正确，没有被过期回收或错误转派。
- 随后同一租约自然完成，服务端接受结果，状态为 `completed`，文章数为 1；Redis owner 和 PostgreSQL claimer 正常释放。
- 完成后节点继续领取下一批 v2 任务，证明不是一次性探针成功。

## 五、服务端与正式同步观察

最终审计时：

- Worker 容器健康，源码挂载只读，协议门禁仍为 `win10-01:2`。
- 灰度窗口内 Worker 日志统计：正式同步成功 28 次、领取 5 次、结果处理 11 次、heartbeat 49 次、Traceback 0、raised 0。
- 本地正式同步 outbox：`sent=18279`，最近 30 分钟发送 810 条；`pending=16`，最老 pending 约 35 秒，符合批量同步的短暂等待状态。
- 同期出现过 1 次其他旧节点的 `owner_changed` 结果拒绝，窗口内不包含 `win10-01`；灰度节点的长租约结果已被接受。因此该事件不阻断本次单机验收，但应作为剩余 v1 节点后续逐台迁移的观察项。

## 六、回滚与残余风险

- Ubuntu 保留发布目录和 compose 覆盖文件，可把 `win10-01` 门禁重新提升为 floor 3 排空，再恢复旧 guest 进程/文件。
- guest 的临时命令文件、一次性 outbox 探针和包含配置回显的临时回执已删除；这些临时文件不可恢复，但都不是业务数据或源码。
- `win10-01` 的筛选面包屑标定、微信登录长驻和重启自恢复没有在本批次重新验收；这三项不能从 v2 协议灰度结果推断为已通过。
- 其余 `win10-02/03/04` 仍是旧协议节点，必须逐台建立身份、排空和回滚证据后再切换。
