# 审核规则集验收记录（2026-08-23）

## 1. 结论

`REVIEW-RULESET-002` 在仓库代码和一次性 PostgreSQL 15 QA 库中验收通过。迁移 024 发布规则集 `review-rules/1.0.0`，但不为真实租户创建 activation；`TENANT_REVIEW_ENABLED` 生产启动门禁保持关闭，本批没有部署、没有写测试服务器或正式服务器。

规则定义 SHA-256 为 `285b8c2fe43ca8b6d3517df223488f4d2fd3e6c7940cbe84ae547ade4b3f48ff`，完成审核只按 Review 内保存的版本、摘要和 JSON 快照校验。`sales_handoff` 仍要求尚未实现的 `opportunity_atomic_create`，因此固定 fail closed，不会把销售就绪伪装成 nurture。

## 2. 本批交付

- 迁移 024：不可变规则正文、完成/重审原因矩阵、租户 activation 历史、Review 规则快照、不可变优先级快照和重审来源约束。
- 审核服务：start/reopen 锁定当前 activation；complete 使用历史快照；结构化原因、未来再评时间、备注和 capability 由服务端校验。
- 重审闭环：仅最新 completed Review 可重开；支持到期重审或前轮完成后新关联的 realtime 来源；最多三轮；同事务保存 Candidate、Review、Outbox 和幂等 receipt。
- 权限：运行 LOGIN 对规则、activation 和评分表只读，只精确获得 active-ruleset 锁函数 EXECUTE；tenant 表继续 FORCE RLS。
- 向前修复：撤销 Grant 只取消未完成 Review、撤回 open/in_review Candidate，不再改写 closed Candidate 或 completed Review。
- 优先级：冻结 `review-priority-envelope/1.0.0`、五项上限和解释码白名单；数据库复核 component 合计与 band，快照保存 evidence refs、input hash、as-of 和规则标识。它只排序、不自动决策，也不冒充尚未标定的确定性 rubric。

## 3. 验收证据

### 仓库与单元门禁

- `tools/check_migrations.py`：24 个连续迁移及冻结 SHA-256 通过。
- 全仓 `pytest -q`：`313 passed, 24 skipped`；跳过项均为需要独立 PostgreSQL/Redis 的 integration marker。
- Review 规则、迁移静态契约、审核服务和运行角色定向测试：`96 passed`。
- Ruff fatal gate `E9,F63,F7,F82`：通过。
- Secret scan：208 个已跟踪文件通过。
- `git diff --check` 与 Python 编译：通过。

### PostgreSQL 15 隔离验收

使用精确命名、仅绑定 `127.0.0.1:55432`、数据目录为 tmpfs 的一次性 `postgres:15-alpine` 容器；库名含 `_lease_qa_` 且同时要求 `ENVIRONMENT=qa`、`ALLOW_DESTRUCTIVE_TESTS=1`。执行结果：

- 从 `docs/db_schema.sql` 起依次应用 001～024：全部成功。
- `python -m wxsearch.migrations.run --check`：24 个已执行 checksum 全部匹配。
- 规则不可变、审核重审、RLS、授权事务、映射 dry-run、非 owner runtime ACL：`23 passed`。
- 实测覆盖 A/B 租户隔离、无上下文零可见、规则/activation/评分写拒绝、精确函数授权、完成审核原子 outbox、新 realtime 来源重审，以及历史来源、完成前已关联来源、错误届次来源的 fail-closed 拒绝。
- 双连接并发测试证明 active ruleset 与 active Grant 的 `FOR SHARE` 锁会阻塞管理侧停用/撤权；同时覆盖幂等重放、旧 Review 不变和撤销 Grant 不改写 completed 历史。

原计划使用 `docker-compose.qa.yml` 全栈执行，但 Docker Hub 获取未缓存的 `python:3.11-slim-bookworm` 时 IPv6 鉴权超时。没有降低数据库验收范围：改用本机已缓存的 PostgreSQL 15 镜像和宿主机隔离测试解释器执行同一迁移器、同一测试文件；失败 Compose 栈与最终临时容器均在验收后销毁。

## 4. 保持关闭的能力

- 不创建真实租户、成员、Grant、Candidate 或 activation。
- 不开启生产/测试环境审核 API，不改现有 SSH 隧道、采集部署或同步链路。
- 不创建 Opportunity，不向销售自动分发；`sales_ready_confirmed` 返回稳定 422，直到商机原子事务完成。
- 不实现自动到期任务、OA 提醒或 SLA 告警；`reopen_not_before` 当前只是可审计门槛。

## 5. 后续入口

下一批优先做 `REVIEW-DISTRIBUTOR-002`（逐租户独立、共享竞争模式的受控候选分发），随后做 `OPPORTUNITY-VERTICAL-SLICE-002`（审核完成与商机/销售交接同事务）。真实上线仍依赖三家公司 roster、预发布恢复演练、Outbox publisher、单租户 canary 和独立发布审批。
