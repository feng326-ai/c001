# 线索质量闸门验收记录（2026-08-23）

## 1. 目标与结论

本批处理“规则草稿和 LLM 失败记录进入正式线索库”的污染链路。代码门禁、测试管理栈、正式 Backend 和本地生产采集 Worker 均已灰度验证；真实业务数据库未执行迁移。

当前安全状态：正式公海默认只展示 `llm_status=done AND has_lead_value=true`；同步发送端与接收端均拒绝未完成清洗的 lead；文章原文继续作为证据保存。业务方已明确授权把新数据和未人工编辑的 pending 正文发送到 DeepSeek，`LLM_CLEAN_ENABLED=true` 已生效；1264 条历史 fail 不重排。

## 2. Git 与自动门禁

- 质量门禁提交：`49bd12b`。
- Worker 恢复保险提交：`5c13191597989832433549edd1f799431395fa48`。
- 模型完整输出预算与验收提交：`3f52292647519a9d26b91875657a39bc291fba8c`。
- `398 passed, 25 skipped`；Ruff 致命规则、`compileall`、25 份迁移仓库完整性与密钥扫描均通过。
- GitHub Web 入口临时不可达时，第二个提交通过 GitHub 官方 Git Database API上传；仅在远端 main 仍指向父提交、tree 与 commit SHA 均和本地完全一致时执行非强制快进。

## 3. 数据处置与门禁口径

- 已为精确关键词“全国 先进 推荐”的 46 条正式 lead 建立压缩 JSONL 备份后删除；正式库剩余 0 条该关键词 lead。
- 来源证据 `articles_core` 保留 156 条，不删除，以支持去重、追溯和以后规则复核。
- 删除前后均核对个人状态与反馈引用，无孤儿记录。
- 正式库发布前基线：`qualified_leads=13346`，其中默认可见 `done+true=7310`，非 done 隔离记录 1423。部署只改变查询口径，没有批量改写这些历史行。
- 本地生产采集库发布前：LLM `done=11923`、`fail=1264`、`pending=1`；同步 Outbox `sent=24142`，无 pending/retry/dead。

## 4. 环境验收

### 测试管理栈

- 镜像：`xiansuo-staging-app:49bd12b`。
- `127.0.0.1:18000/health` 与 `https://leads.yixiua.com/health` 均健康。
- 默认查询在容器内回读为 `llm_status='done' AND has_lead_value=TRUE`；pending 接收判定为 false、done 为 true。
- 仅测试 Backend 容器重建；测试 PostgreSQL/Redis 和全部真实采集容器 ID 未变化。

### 正式业务栈

- Backend 代码与 docs 来自 `/home/ubuntu/releases/49bd12b`，均为只读挂载；日志目录保持可写持久化。
- `127.0.0.1:8000/health` 与 `https://c.yfpoll.cn/health` 均健康；日志无 Traceback/Error。
- 仅 Backend 重建；正式 PostgreSQL、Redis、Worker、Beat 容器 ID 未变化。
- Compose v1 首次原地 recreate 触发 `ContainerConfig` 兼容故障，原容器随即恢复健康；随后删除并只重建无状态 Backend，发布成功。数据容器和卷从未停止或删除。

### 本地生产采集栈

- Worker 来自只读 Git release `3f52292647519a9d26b91875657a39bc291fba8c`。
- 密钥目录 `/home/m/runtime-secrets/wxsearch` 为 0700、`secrets.json` 为 0600，并只读挂载到 `/run/secrets/wxsearch`；容器只验证 key 存在，未回显值。
- `LLM_CLEAN_ENABLED=true`；`CLAIM_PROTOCOL_FLOORS=win10-01:2` 保持不变。
- 发送端回读：pending lead 不可发布，done lead 可发布；Worker 健康且无 401/Traceback。
- 仅 Worker 重建；Beat、Backend、PostgreSQL、Redis、测试栈容器 ID 未变化。

## 5. 模型合成验收与停止点

- 全部模型探测只使用明确标注的合成活动文本，没有读取或发送真实采集正文。
- 密钥有效且配置模型 `deepseek-v4-flash` 位于账户可用模型列表。
- 2048 输出上限实测 `finish_reason=length`、`completion_tokens=2048`，会截断结构化正文；4096 实测 `finish_reason=stop`、JSON 可解析且包含 23 个字段。
- `llm.max_tokens=4096` 的 Git release 已部署；容器内端到端 `analyze()` 合成契约验收成功，返回 24 个规范化字段。
- 业务方明确授权：“仅恢复新数据和 pending 数据，暂不重跑 1264 条历史失败记录”。随后启用清洗并观察 15:31、15:32、15:33 三个连续周期，任务均成功、无 401/截断/熔断，历史 fail 数量 1264 与尝试次数总和 4168 均不变。
- 唯一 pending（ID 1009）为 `updated_by_human=true`，被查询条件正确排除，未发送正文、未覆盖人工结果。观察期间没有新的合格 pending，因此没有真实正文外呼；以后新入库且未人工编辑的 pending 才会自动发送到 DeepSeek。

## 6. 回滚点

- 测试 Backend：镜像 `xiansuo-staging-app:1ba3e7a`。
- 正式 Backend：base-only `docker-compose.prod.yml` 的原现场源码挂载。
- 采集 Worker：优先回到仍默认关闭 LLM 的 release `5c13191597989832433549edd1f799431395fa48`；必要时再回到 `75c451ff4e90a7eda849b80c043c7378e2f4c106`。
- 回滚均只允许单服务重建；禁止 `down -v`、删除数据库/Redis 卷、覆盖现场库或回滚 020 迁移。
