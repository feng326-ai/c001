[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers
markers =
    integration: requires disposable PostgreSQL/Redis or an external service
    slow: intentionally slower than the PR fast gate
